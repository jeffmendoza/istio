<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

#<match **>
#  @type stdout
#</match>

<match **.istio-system>
  @type file
  path /conf/out
  buffer_type file
  buffer_path /conf/buf
  flush_interval 1s
  flush_at_shutdown true
</match>
